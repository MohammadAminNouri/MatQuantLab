exec('\nfrom __future__ import annotations\n\nimport time\nimport datetime as dt\nfrom pathlib import Path\nfrom urllib.parse import quote_plus\nimport xml.etree.ElementTree as ET\nimport warnings\n\nimport numpy as np\nimport pandas as pd\nimport plotly.graph_objects as go\nimport requests\nimport streamlit as st\nimport yfinance as yf\n\nfrom sklearn.linear_model import Ridge, HuberRegressor, ElasticNet\nfrom sklearn.ensemble import ExtraTreesRegressor, RandomForestRegressor, GradientBoostingRegressor, VotingRegressor\nfrom sklearn.metrics import mean_squared_error\n\nwarnings.filterwarnings("ignore")\nROOT = Path(__file__).resolve().parents[1]\n\n# -----------------------------------------------------------------------------\n# Domain map: materials, futures, manufacturing assets\n# -----------------------------------------------------------------------------\nMATERIALS = {\n    "Copper": {\n        "tickers": ["HG=F", "CPER", "COPX"],\n        "family": "industrial metals",\n        "keywords": ["copper", "grid", "electrification", "China demand", "mine supply"],\n        "producer_positive": "higher copper usually helps miners/producers, but can hurt manufacturers using copper-heavy components",\n        "manufacturer_risk": "input-cost pressure for motors, electronics, wiring, heat exchangers and grid equipment",\n    },\n    "Aluminum": {\n        "tickers": ["ALI=F", "AA"],\n        "family": "light metals",\n        "keywords": ["aluminum", "aluminium", "alumina", "bauxite", "smelter"],\n        "producer_positive": "higher aluminum can support producers but usually raises cost for transport/aerospace users",\n        "manufacturer_risk": "cost pressure for aerospace structures, automotive lightweighting, packaging and heat exchangers",\n    },\n    "Nickel": {\n        "tickers": ["JJN", "VALE", "NILSY"],\n        "family": "superalloys and batteries",\n        "keywords": ["nickel", "superalloy", "stainless steel", "battery cathode"],\n        "producer_positive": "higher nickel can support miners/refiners",\n        "manufacturer_risk": "cost pressure for stainless steel, superalloys, batteries and turbine materials",\n    },\n    "Titanium": {\n        "tickers": ["ATI", "TROX", "CRS"],\n        "family": "aerospace and AM powder feedstock",\n        "keywords": ["titanium", "titanium sponge", "Ti-6Al-4V", "titanium powder", "aerospace"],\n        "producer_positive": "supports specialty-material suppliers if demand is healthy",\n        "manufacturer_risk": "cost and availability risk for aerospace, medical implants and metal AM powder users",\n    },\n    "Steel chain": {\n        "tickers": ["SLX", "TIO=F", "HCC", "MT", "NUE"],\n        "family": "steel and PM inputs",\n        "keywords": ["steel", "iron ore", "coking coal", "scrap", "powder metallurgy"],\n        "producer_positive": "can support steel producers when demand is strong",\n        "manufacturer_risk": "cost pressure for machinery, autos, construction equipment and powder-metallurgy input chains",\n    },\n    "Lithium": {\n        "tickers": ["LIT", "ALB", "SQM", "PLL"],\n        "family": "battery materials",\n        "keywords": ["lithium", "spodumene", "battery", "EV"],\n        "producer_positive": "supports lithium miners/processors when demand and pricing improve",\n        "manufacturer_risk": "battery-cost pressure for EVs and storage if the move is supply-driven",\n    },\n    "Cobalt": {\n        "tickers": ["COB.AX", "GLEN.L", "VALE"],\n        "family": "battery materials",\n        "keywords": ["cobalt", "cathode", "DRC", "battery supply"],\n        "producer_positive": "can support producers/refiners",\n        "manufacturer_risk": "supply concentration and cathode-cost risk",\n    },\n    "Graphite": {\n        "tickers": ["NMG", "SYAAF", "TGR.AX"],\n        "family": "battery and PM inputs",\n        "keywords": ["graphite", "anode", "battery anode", "export controls"],\n        "producer_positive": "can support graphite names if demand is confirmed",\n        "manufacturer_risk": "battery anode and export-control risk; also relevant to some powder/process chains",\n    },\n    "Rare earths": {\n        "tickers": ["REMX", "MP", "LYC.AX"],\n        "family": "critical minerals",\n        "keywords": ["rare earth", "neodymium", "dysprosium", "magnet", "NdFeB", "export controls"],\n        "producer_positive": "can support rare-earth producers if policy/demand improves",\n        "manufacturer_risk": "magnet, EV motor, defense electronics and semiconductor-equipment supply risk",\n    },\n    "Uranium": {\n        "tickers": ["URA", "URNM", "CCJ"],\n        "family": "energy security",\n        "keywords": ["uranium", "nuclear fuel", "enrichment", "energy security"],\n        "producer_positive": "can support uranium miners/fuel-cycle names",\n        "manufacturer_risk": "less direct manufacturing cost; more energy-security and power-price channel",\n    },\n    "Oil": {\n        "tickers": ["CL=F", "BZ=F", "USO", "XLE"],\n        "family": "energy and logistics",\n        "keywords": ["oil", "crude", "OPEC", "fuel", "refinery", "Middle East"],\n        "producer_positive": "supports energy producers when demand-driven",\n        "manufacturer_risk": "fuel, logistics, petrochemical and inflation pressure for manufacturers",\n    },\n    "Natural gas": {\n        "tickers": ["NG=F", "UNG"],\n        "family": "process energy",\n        "keywords": ["natural gas", "LNG", "power price", "heat treatment", "HIP", "atomization"],\n        "producer_positive": "supports gas producers when demand-driven",\n        "manufacturer_risk": "direct process-energy pressure: atomization, heat treatment, HIP, smelting and chemicals",\n    },\n    "Gold": {\n        "tickers": ["GC=F", "GLD"],\n        "family": "safe haven and real rates",\n        "keywords": ["gold", "bullion", "central bank gold", "safe haven", "real rates"],\n        "producer_positive": "supports gold miners and gold proxies",\n        "manufacturer_risk": "not a manufacturing input signal; more a stress/real-rate signal",\n    },\n    "Silver": {\n        "tickers": ["SI=F", "SLV"],\n        "family": "precious/industrial hybrid",\n        "keywords": ["silver", "solar", "electronics", "precious metal"],\n        "producer_positive": "supports silver miners/proxies",\n        "manufacturer_risk": "industrial input signal for solar/electronics, also safe-haven behavior",\n    },\n    "Platinum": {\n        "tickers": ["PL=F", "PPLT"],\n        "family": "catalysts and hydrogen",\n        "keywords": ["platinum", "catalyst", "hydrogen", "fuel cell"],\n        "producer_positive": "supports PGM producers/proxies",\n        "manufacturer_risk": "auto-catalyst and hydrogen supply-chain cost channel",\n    },\n    "Palladium": {\n        "tickers": ["PA=F", "PALL"],\n        "family": "auto catalysts",\n        "keywords": ["palladium", "auto catalyst", "Russia supply"],\n        "producer_positive": "supports PGM producers/proxies",\n        "manufacturer_risk": "auto-catalyst input and supply-risk channel",\n    },\n}\n\nFUTURES = {\n    "WTI crude oil": "CL=F",\n    "Brent oil": "BZ=F",\n    "Natural gas": "NG=F",\n    "Copper": "HG=F",\n    "Gold": "GC=F",\n    "Silver": "SI=F",\n    "Platinum": "PL=F",\n    "Palladium": "PA=F",\n    "Iron ore proxy": "TIO=F",\n    "US dollar index": "DX-Y.NYB",\n    "VIX volatility": "^VIX",\n    "US 10Y yield": "^TNX",\n    "Gold ETF": "GLD",\n    "Silver ETF": "SLV",\n    "Oil ETF": "USO",\n    "Natural gas ETF": "UNG",\n    "Uranium ETF": "URA",\n    "Rare earth ETF": "REMX",\n    "Lithium/battery ETF": "LIT",\n    "Steel ETF": "SLX",\n}\n\nCORE = {\n    "SPY": ("SPDR S&P 500 ETF", "broad market", "market beta, risk appetite"),\n    "XME": ("SPDR Metals & Mining ETF", "mining and metals", "steel, copper, energy, China demand"),\n    "COPX": ("Global X Copper Miners ETF", "copper miners", "copper, USD, energy, China demand"),\n    "ITA": ("iShares U.S. Aerospace & Defense ETF", "aerospace / defense", "titanium, aluminum, nickel, defense procurement, energy"),\n    "XAR": ("SPDR Aerospace & Defense ETF", "aerospace supply chain", "titanium, aluminum, nickel, defense procurement, energy"),\n    "PRNT": ("3D Printing ETF", "additive manufacturing", "titanium powder, nickel, aluminum, LPBF, EBM, HIP, aerospace demand"),\n    "SOXX": ("iShares Semiconductor ETF", "semiconductors", "rare earths, Taiwan risk, electronics, USD"),\n    "GLD": ("SPDR Gold Shares", "gold", "safe haven, real rates, geopolitical risk"),\n    "SLV": ("iShares Silver Trust", "silver", "precious/industrial hybrid, solar, electronics"),\n    "USO": ("United States Oil Fund", "oil", "fuel, Middle East, inflation"),\n    "UNG": ("United States Natural Gas Fund", "natural gas", "LNG, power, process energy"),\n    "URA": ("Global X Uranium ETF", "uranium", "nuclear fuel, energy security"),\n    "LIT": ("Global X Lithium & Battery Tech ETF", "battery materials", "lithium, nickel, cobalt, graphite, EV demand"),\n    "REMX": ("VanEck Rare Earth/Strategic Metals ETF", "rare earths", "rare earths, magnets, export controls"),\n    "SLX": ("VanEck Steel ETF", "steel", "iron ore, coking coal, steel margins"),\n}\n\nDEFAULT_COMPANIES = pd.DataFrame([\n    ["ATI", "ATI Inc.", "United States", "specialty alloys", "titanium, nickel, aerospace, AM powder feedstock"],\n    ["GE", "GE Aerospace", "United States", "aerospace", "jet engines, nickel superalloys, titanium"],\n    ["BA", "Boeing", "United States", "aerospace", "aircraft, aluminum, titanium, supply chain"],\n    ["RTX", "RTX", "United States", "aerospace/defense", "engines, missiles, defense electronics"],\n    ["LMT", "Lockheed Martin", "United States", "defense", "defense procurement, rare earths, titanium"],\n    ["NUE", "Nucor", "United States", "steel", "steel, scrap, construction"],\n    ["AA", "Alcoa", "United States", "aluminum", "aluminum, smelting power cost"],\n    ["FCX", "Freeport-McMoRan", "United States", "copper mining", "copper supply and demand"],\n    ["AIR.PA", "Airbus", "France", "aerospace", "aircraft, aluminum, titanium"],\n    ["SAF.PA", "Safran", "France", "aerospace", "jet engines, nickel, titanium"],\n    ["HO.PA", "Thales", "France", "defense electronics", "rare earths, semiconductors, defense"],\n    ["RR.L", "Rolls-Royce", "United Kingdom", "aerospace", "jet engines, nickel superalloys"],\n    ["BA.L", "BAE Systems", "United Kingdom", "defense", "defense systems, rare earths"],\n    ["RIO.L", "Rio Tinto", "United Kingdom", "mining", "iron ore, aluminum, copper"],\n    ["GLEN.L", "Glencore", "United Kingdom", "mining/trading", "copper, cobalt, coal"],\n    ["RHM.DE", "Rheinmetall", "Germany", "defense", "munitions, steel, defense procurement"],\n    ["SIE.DE", "Siemens", "Germany", "industrial automation", "electrification, copper, automation"],\n    ["BAS.DE", "BASF", "Germany", "chemicals", "natural gas, feedstock, chemicals"],\n    ["VOW3.DE", "Volkswagen", "Germany", "autos", "steel, aluminum, batteries"],\n    ["ASML.AS", "ASML", "Netherlands", "semiconductor equipment", "semiconductor equipment, rare earths, Taiwan"],\n    ["1211.HK", "BYD", "China", "EV/batteries", "lithium, batteries, rare earths"],\n    ["2899.HK", "Zijin Mining", "China", "mining", "copper, gold, lithium"],\n    ["600019.SS", "Baoshan Iron & Steel", "China", "steel", "steel and China construction"],\n    ["7203.T", "Toyota", "Japan", "autos", "steel, aluminum, batteries"],\n    ["7011.T", "Mitsubishi Heavy Industries", "Japan", "heavy industry/aerospace", "aerospace, defense, energy systems"],\n    ["5713.T", "Sumitomo Metal Mining", "Japan", "mining/materials", "nickel, copper, battery materials"],\n    ["005930.KS", "Samsung Electronics", "South Korea", "semiconductors", "semiconductors, memory, electronics"],\n    ["373220.KS", "LG Energy Solution", "South Korea", "batteries", "lithium, nickel, cobalt"],\n    ["005490.KS", "POSCO Holdings", "South Korea", "steel/materials", "steel, battery materials"],\n    ["2330.TW", "TSMC", "Taiwan", "semiconductors", "semiconductors, Taiwan risk"],\n    ["BHP.AX", "BHP", "Australia", "mining", "iron ore, copper, coal"],\n    ["LYC.AX", "Lynas Rare Earths", "Australia", "rare earths", "rare earths, NdPr"],\n    ["PLS.AX", "Pilbara Minerals", "Australia", "lithium", "lithium, spodumene"],\n], columns=["ticker", "company", "country", "industry_group", "material_link"])\n\n# -----------------------------------------------------------------------------\n# Loading and utility\n# -----------------------------------------------------------------------------\ndef load_companies() -> pd.DataFrame:\n    p = Path("data/manufacturing_company_universe.csv")\n    try:\n        if p.exists():\n            df = pd.read_csv(p)\n            if not df.empty:\n                needed = ["ticker", "company", "country", "industry_group", "material_link"]\n                for c in needed:\n                    if c not in df.columns:\n                        df[c] = ""\n                return df\n    except Exception:\n        pass\n    return DEFAULT_COMPANIES\n\nCOMPANIES = load_companies()\n\ndef compatible_period(interval: str, period: str) -> str:\n    if interval == "1m":\n        return period if period in ["1d", "5d"] else "5d"\n    if interval in ["5m", "15m", "30m", "60m"]:\n        return period if period in ["1d", "5d", "1mo", "3mo", "6mo"] else "60d"\n    return period\n\ndef interval_delta(interval: str, bars: int) -> dt.timedelta:\n    if interval.endswith("m"):\n        return dt.timedelta(minutes=int(interval[:-1]) * bars)\n    return dt.timedelta(days=bars)\n\ndef fmt_pct(x):\n    if pd.isna(x):\n        return ""\n    return f"{x*100:.2f}%"\n\ndef zscore(s: pd.Series, window: int) -> pd.Series:\n    window = max(3, int(window))\n    m = s.rolling(window, min_periods=max(3, window // 3)).mean()\n    sd = s.rolling(window, min_periods=max(3, window // 3)).std()\n    return ((s - m) / sd).replace([np.inf, -np.inf], np.nan)\n\ndef asset_info(ticker: str):\n    if ticker in CORE:\n        name, group, link = CORE[ticker]\n        return name, group, link\n    r = COMPANIES[COMPANIES["ticker"].astype(str) == ticker]\n    if not r.empty:\n        x = r.iloc[0]\n        return str(x["company"]), str(x["industry_group"]), str(x["material_link"])\n    for k, v in FUTURES.items():\n        if v == ticker:\n            return k, "commodity / future / proxy", k\n    return ticker, "selected instrument", ticker\n\ndef relevant_materials_for_asset(ticker: str) -> list[str]:\n    name, group, link = asset_info(ticker)\n    text = f"{ticker} {name} {group} {link}".lower()\n    hits = []\n    for mat, spec in MATERIALS.items():\n        if mat.lower() in text or any(k.lower() in text for k in spec["keywords"]):\n            hits.append(mat)\n    # sensible fallbacks\n    if not hits:\n        if "aerospace" in text or "defense" in text:\n            hits = ["Titanium", "Aluminum", "Nickel", "Rare earths", "Oil", "Natural gas"]\n        elif "mining" in text:\n            hits = ["Copper", "Steel chain", "Oil", "Gold"]\n        elif "semiconductor" in text:\n            hits = ["Rare earths", "Silver", "Copper", "Natural gas"]\n        elif "gold" in text:\n            hits = ["Gold", "US dollar index"]\n    return [h for h in hits if h in MATERIALS]\n\ndef build_news_terms(base_terms: list[str], assets: list[str]) -> list[str]:\n    terms = list(base_terms)\n    for a in assets:\n        name, group, link = asset_info(a)\n        terms += [a, name, group, link]\n        for m in relevant_materials_for_asset(a):\n            terms += MATERIALS[m]["keywords"] + [m]\n    clean = []\n    seen = set()\n    for t in terms:\n        for p in str(t).replace("/", " ").split(","):\n            p = p.strip()\n            if p and p.lower() not in seen and len(p) < 80:\n                clean.append(p)\n                seen.add(p.lower())\n    return clean[:70]\n\ndef make_query(terms: list[str]) -> str:\n    return "(" + " OR ".join(terms) + ")" if terms else "(critical minerals OR oil OR copper)"\n\n@st.cache_data(ttl=45, show_spinner=False)\ndef download_prices(tickers, period, interval):\n    tickers = sorted(set([t for t in tickers if isinstance(t, str) and t.strip()]))\n    if not tickers:\n        return pd.DataFrame(), []\n    raw = yf.download(tickers, period=period, interval=interval, auto_adjust=True, progress=False, threads=True)\n    if raw.empty:\n        return pd.DataFrame(), tickers\n    if isinstance(raw.columns, pd.MultiIndex):\n        close = raw["Close"].copy() if "Close" in raw.columns.get_level_values(0) else raw.xs("Close", axis=1, level=1, drop_level=True).copy()\n    else:\n        close = raw[["Close"]].copy()\n        if len(tickers) == 1:\n            close.columns = tickers\n    close = close.dropna(axis=1, how="all").ffill().dropna(how="all")\n    missing = [t for t in tickers if t not in close.columns]\n    return close, missing\n\n@st.cache_data(ttl=360, show_spinner=False)\ndef gdelt_news(query: str, n: int = 60):\n    try:\n        r = requests.get(\n            "https://api.gdeltproject.org/api/v2/doc/doc",\n            params={"query": query, "mode": "ArtList", "format": "json", "maxrecords": n, "sort": "HybridRel"},\n            timeout=10,\n        )\n        if r.status_code != 200:\n            return pd.DataFrame()\n        return pd.DataFrame([{"title": a.get("title", ""), "source": a.get("domain", ""), "date": a.get("seendate", ""), "url": a.get("url", "")} for a in r.json().get("articles", [])])\n    except Exception:\n        return pd.DataFrame()\n\n@st.cache_data(ttl=900, show_spinner=False)\ndef google_news(query: str, n: int = 50):\n    try:\n        url = "https://news.google.com/rss/search?q=" + quote_plus(query) + "&hl=en-US&gl=US&ceid=US:en"\n        r = requests.get(url, timeout=10, headers={"User-Agent": "MatQuantLab"})\n        if r.status_code != 200:\n            return pd.DataFrame()\n        root = ET.fromstring(r.content)\n        rows = []\n        for item in root.findall(".//item")[:n]:\n            rows.append({"title": (item.findtext("title") or "").strip(), "source": "Google News", "date": (item.findtext("pubDate") or "").strip(), "url": (item.findtext("link") or "").strip()})\n        return pd.DataFrame(rows)\n    except Exception:\n        return pd.DataFrame()\n\ndef returns_table(prices: pd.DataFrame, assets: list[str]) -> pd.DataFrame:\n    rows = []\n    for a in assets:\n        if a not in prices.columns:\n            continue\n        s = prices[a].dropna()\n        if s.empty:\n            continue\n        name, group, link = asset_info(a)\n        last = float(s.iloc[-1])\n        def ret(b):\n            if len(s) <= b:\n                return np.nan\n            return float(s.iloc[-1] / s.iloc[-1-b] - 1)\n        vol = float(s.pct_change().tail(20).std() * np.sqrt(252)) if len(s) > 25 else np.nan\n        z20 = float(zscore(s.pct_change(), 20).dropna().iloc[-1]) if len(s) > 25 and not zscore(s.pct_change(), 20).dropna().empty else np.nan\n        draw = float(s.iloc[-1] / s.rolling(min(63, len(s))).max().iloc[-1] - 1) if len(s) > 5 else np.nan\n        rows.append({\n            "asset": a,\n            "name": name,\n            "group": group,\n            "last price": last,\n            "1 bar": ret(1),\n            "5 bars": ret(5),\n            "20 bars": ret(20),\n            "realized vol": vol,\n            "z-score": z20,\n            "drawdown": draw,\n            "state": "extended up" if z20 > 1 else "extended down" if z20 < -1 else "normal",\n        })\n    return pd.DataFrame(rows)\n\ndef build_features(prices: pd.DataFrame, selected_assets: list[str], shock_window: int):\n    n = len(prices)\n    w = max(3, min(shock_window, max(3, n // 5))) if n < 200 else max(5, min(shock_window, 100))\n    lag = max(1, min(w, max(1, n // 4)))\n    features = pd.DataFrame(index=prices.index)\n    meta = []\n    # selected asset move features\n    for a in selected_assets:\n        if a in prices.columns:\n            c = f"asset:{a}"\n            features[c] = zscore(prices[a].pct_change(lag), w)\n            meta.append({"feature": c, "source": a, "meaning": f"normalized recent move in selected asset {a}"})\n    # relevant material features\n    mats = []\n    for a in selected_assets:\n        mats += relevant_materials_for_asset(a)\n    if not mats:\n        mats = list(MATERIALS.keys())\n    for m in sorted(set(mats)):\n        chosen = next((t for t in MATERIALS[m]["tickers"] if t in prices.columns), None)\n        if chosen:\n            c = f"material:{m}"\n            features[c] = zscore(prices[chosen].pct_change(lag), w)\n            meta.append({"feature": c, "source": chosen, "meaning": MATERIALS[m]["family"] + " — " + MATERIALS[m]["manufacturer_risk"]})\n    # essential macro/market\n    for label, ticker in {"VIX": "^VIX", "USD": "DX-Y.NYB", "Gold": "GC=F", "Oil": "CL=F", "Gas": "NG=F", "Copper": "HG=F"}.items():\n        if ticker in prices.columns:\n            c = f"macro:{label}"\n            features[c] = zscore(prices[ticker].pct_change(lag), w)\n            meta.append({"feature": c, "source": ticker, "meaning": f"{label} macro/material context"})\n    # AM/PM engineered features\n    def mean_cols(keys):\n        cols = [k for k in keys if k in features.columns]\n        return features[cols].mean(axis=1) if cols else pd.Series(index=features.index, dtype=float)\n    features["AM-PM:feedstock"] = mean_cols(["material:Titanium", "material:Nickel", "material:Aluminum", "material:Rare earths"])\n    features["AM-PM:process energy"] = mean_cols(["material:Natural gas", "macro:Gas", "macro:Oil"])\n    features["AM-PM:powder metallurgy inputs"] = mean_cols(["material:Steel chain", "material:Graphite", "material:Nickel", "material:Tungsten"])\n    am_cols = [c for c in features.columns if c.startswith("AM-PM:")]\n    if am_cols:\n        features["AM-PM:basket"] = features[am_cols].mean(axis=1)\n    mat_cols = [c for c in features.columns if c.startswith("material:")]\n    features["material stress"] = features[mat_cols].mean(axis=1) if mat_cols else features.mean(axis=1)\n    features = features.replace([np.inf, -np.inf], np.nan).ffill().fillna(0)\n    return features, pd.DataFrame(meta), w\n\ndef current_driver_table(features: pd.DataFrame, selected_assets: list[str]):\n    latest = features.iloc[-1]\n    rows = []\n    for c, v in latest.items():\n        v = float(v)\n        if c.startswith("asset:"):\n            channel = "selected asset momentum"\n        elif c.startswith("material:"):\n            channel = "material / input"\n        elif c.startswith("AM-PM:"):\n            channel = "AM / powder metallurgy"\n        elif c.startswith("macro:"):\n            channel = "macro / hedge"\n        else:\n            channel = "system"\n        rows.append({\n            "driver": c,\n            "channel": channel,\n            "score": v,\n            "absolute": abs(v),\n            "reading": "positive / upward shock" if v > .75 else "negative / easing shock" if v < -.75 else "near normal",\n        })\n    df = pd.DataFrame(rows).sort_values("absolute", ascending=False)\n    return df\n\ndef material_interpretation(driver: str, score: float, focus_asset: str):\n    direction = "upward pressure" if score > 0 else "easing/downward pressure"\n    mag = "large" if abs(score) >= 1.5 else "moderate" if abs(score) >= .75 else "small"\n    name, group, link = asset_info(focus_asset)\n    if driver.startswith("material:"):\n        mat = driver.split(":", 1)[1]\n        spec = MATERIALS.get(mat, {})\n        if "miner" in group or "mining" in group or mat.lower() in group.lower():\n            meaning = spec.get("producer_positive", "can support producers if demand-driven")\n        else:\n            meaning = spec.get("manufacturer_risk", "can transmit into input-cost or supply-chain risk")\n        return f"{driver}: {mag} {direction}. For {focus_asset} ({group}), this mainly means: {meaning}."\n    if driver.startswith("macro:USD"):\n        return f"{driver}: {mag} {direction}. A stronger dollar often pressures commodities and emerging-market demand; a weaker dollar often supports commodity pricing."\n    if driver.startswith("macro:VIX"):\n        return f"{driver}: {mag} {direction}. Higher VIX usually means weaker risk appetite; lower VIX supports cyclical and growth-sensitive assets."\n    if driver.startswith("AM-PM"):\n        return f"{driver}: {mag} {direction}. This is relevant to metal powder, LPBF/EBM/DED, HIP, heat treatment and aerospace/medical/defense adoption."\n    return f"{driver}: {mag} {direction}. It is included because it is directly tied to the current selected universe."\n\ndef chart_lines(df, title, ytitle="price", normalize=False):\n    if df.empty:\n        st.info("No data for this chart under the current selection.")\n        return\n    x = df.copy().dropna(how="all").ffill()\n    if normalize:\n        x = x / x.iloc[0]\n    fig = go.Figure()\n    for c in x.columns:\n        fig.add_trace(go.Scatter(x=x.index, y=x[c], name=str(c), mode="lines", hovertemplate=f"{c}<br>%{{x}}<br>%{{y:.4f}}<extra></extra>"))\n    fig.update_layout(title=title, height=420, xaxis_title="time", yaxis_title=ytitle, hovermode="x unified", margin=dict(l=20, r=20, t=55, b=30))\n    st.plotly_chart(fig, use_container_width=True)\n\ndef chart_bar(df, x, y, title, color_col=None):\n    if df.empty:\n        st.info("No data for this chart under the current selection.")\n        return\n    d = df.copy().sort_values(x)\n    fig = go.Figure()\n    fig.add_trace(go.Bar(x=d[x], y=d[y], orientation="h", text=[f"{v:.2f}" for v in d[x]], textposition="auto"))\n    fig.update_layout(title=title, height=max(350, 28 * len(d)), xaxis_title=x, yaxis_title="", margin=dict(l=20, r=20, t=55, b=30))\n    st.plotly_chart(fig, use_container_width=True)\n\n# -----------------------------------------------------------------------------\n# Model\n# -----------------------------------------------------------------------------\ndef models():\n    return {\n        "Ridge": Ridge(alpha=8),\n        "ElasticNet": ElasticNet(alpha=.01, l1_ratio=.2, random_state=42),\n        "Huber": HuberRegressor(epsilon=1.35, alpha=.0001),\n        "RandomForest": RandomForestRegressor(n_estimators=100, max_depth=4, random_state=42, n_jobs=-1),\n        "ExtraTrees": ExtraTreesRegressor(n_estimators=140, max_depth=5, random_state=42, n_jobs=-1),\n        "GradientBoosting": GradientBoostingRegressor(max_depth=2, random_state=42),\n        "Blended": VotingRegressor([\n            ("ridge", Ridge(alpha=8)),\n            ("trees", ExtraTreesRegressor(n_estimators=80, max_depth=4, random_state=42, n_jobs=-1)),\n            ("gb", GradientBoostingRegressor(max_depth=2, random_state=42)),\n        ]),\n    }\n\ndef fit_model(model, Xtr, ytr, Xte, yte):\n    model.fit(Xtr, ytr)\n    p = model.predict(Xte)\n    corr = pd.Series(p).corr(pd.Series(yte.values))\n    corr = 0 if pd.isna(corr) else float(corr)\n    rmse = float(mean_squared_error(yte, p) ** .5)\n    vol = max(float(ytr.std()), 1e-8)\n    score = corr - .1 * min(rmse / vol, 5)\n    return model, p, corr, rmse, score\n\ndef importance(model, cols):\n    if hasattr(model, "feature_importances_"):\n        return pd.Series(model.feature_importances_, index=cols).sort_values(ascending=False)\n    if hasattr(model, "coef_"):\n        co = np.array(model.coef_).reshape(-1)\n        return pd.Series(np.abs(co), index=cols[:len(co)]).sort_values(ascending=False)\n    if hasattr(model, "estimators_"):\n        parts = []\n        for e in model.estimators_:\n            if hasattr(e, "feature_importances_"):\n                parts.append(pd.Series(e.feature_importances_, index=cols))\n            elif hasattr(e, "coef_"):\n                co = np.array(e.coef_).reshape(-1)\n                parts.append(pd.Series(np.abs(co), index=cols[:len(co)]))\n        if parts:\n            return pd.concat(parts, axis=1).mean(axis=1).sort_values(ascending=False)\n    return pd.Series(0, index=cols)\n\ndef train_signal(asset, prices, features, horizon, interval, model_choice):\n    if asset not in prices.columns:\n        return None, None\n    px = prices[asset].dropna()\n    if len(px) < 20:\n        return None, None\n    h = max(1, min(int(horizon), max(1, min(len(px), len(features)) // 8)))\n    def dataset(hh):\n        y = prices[asset].pct_change(hh).shift(-hh).rename("future_return")\n        X = features.copy()\n        X = X.loc[:, X.std() > 1e-9]\n        return pd.concat([X, y], axis=1, join="inner").dropna(subset=["future_return"])\n    df = dataset(h)\n    if len(df) < 30:\n        h = max(1, min(5, horizon))\n        df = dataset(h)\n    if len(df) < 18:\n        h = 1\n        df = dataset(1)\n    if len(df) < 14:\n        return None, None\n    cols = [c for c in df.columns if c != "future_return"]\n    df[cols] = df[cols].fillna(0)\n    split = max(8, int(len(df) * .70))\n    if split >= len(df) - 4:\n        split = max(6, len(df) - 5)\n    tr, te = df.iloc[:split], df.iloc[split:]\n    if len(te) < 3:\n        return None, None\n    Xtr, ytr, Xte, yte = tr[cols], tr["future_return"], te[cols], te["future_return"]\n    mdls = models()\n    if model_choice == "Adaptive":\n        best = None\n        for nm, m in mdls.items():\n            try:\n                fm, p, corr, rmse, score = fit_model(m, Xtr, ytr, Xte, yte)\n                if best is None or score > best["score"]:\n                    best = {"name": nm, "model": fm, "pred": p, "corr": corr, "rmse": rmse, "score": score}\n            except Exception:\n                pass\n        if best is None:\n            return None, None\n        model, pred, corr, rmse, model_name = best["model"], best["pred"], best["corr"], best["rmse"], best["name"]\n    else:\n        try:\n            model, pred, corr, rmse, _ = fit_model(mdls[model_choice], Xtr, ytr, Xte, yte)\n            model_name = model_choice\n        except Exception:\n            return None, None\n    latest_pred = float(model.predict(features.iloc[[-1]][cols].fillna(0))[0])\n    vol = max(float(ytr.std()), 1e-8)\n    confidence = min(abs(latest_pred) / (vol * 1.25), 1)\n    direction = "positive" if latest_pred > 0 else "negative"\n    strength = "strong" if confidence > .66 else "moderate" if confidence > .33 else "weak"\n    if len(te) < 8:\n        trust = "low"\n        trust_reason = "few test observations"\n    elif corr > .12 and confidence > .50:\n        trust = "higher"\n        trust_reason = "validation correlation and signal size are both acceptable"\n    elif corr > .03 and confidence > .33:\n        trust = "medium"\n        trust_reason = "validation correlation is positive but not strong"\n    elif corr > 0:\n        trust = "low-medium"\n        trust_reason = "validation is positive but weak"\n    else:\n        trust = "low"\n        trust_reason = "validation did not support direction"\n    if trust in ["higher", "medium"] and strength in ["strong", "moderate"]:\n        stance = "constructive pressure" if direction == "positive" else "downside pressure"\n    elif direction == "negative" and strength in ["strong", "moderate"]:\n        stance = "risk watch"\n    elif direction == "positive" and strength in ["strong", "moderate"]:\n        stance = "mild constructive pressure"\n    else:\n        stance = "no reliable edge"\n    imp = importance(model, cols)\n    drivers = list(imp.index[:3])\n    try:\n        until = pd.Timestamp(px.index[-1]) + interval_delta(interval, h)\n        until_s = str(until)[:16]\n    except Exception:\n        until_s = "next horizon window"\n    invalid = "; ".join([f"{d} returns toward 0 or flips sign" for d in drivers[:2]]) if drivers else "top drivers reverse"\n    validity = f"Useful until about {until_s}, or earlier if {invalid}."\n    message = f"{asset}: {stance}. Forecast horizon is {h} {interval} bars. Trust is {trust}: {trust_reason}. Main drivers: {\', \'.join(drivers)}. {validity}"\n    curve = te.copy()\n    curve["prediction"] = pred\n    curve["future_return"] = yte\n    curve["diagnostic_curve"] = (1 + np.sign(curve["prediction"]) * curve["future_return"].fillna(0)).cumprod()\n    row = {\n        "asset": asset,\n        "model": model_name,\n        "stance": stance,\n        "direction": direction,\n        "strength": strength,\n        "trust": trust,\n        "prediction": latest_pred,\n        "confidence": confidence,\n        "test_corr": corr,\n        "rmse": rmse,\n        "horizon_bars": h,\n        "valid_until": until_s,\n        "validity": validity,\n        "driver_1": drivers[0] if len(drivers) > 0 else "",\n        "driver_2": drivers[1] if len(drivers) > 1 else "",\n        "driver_3": drivers[2] if len(drivers) > 2 else "",\n        "data_start": str(pd.Timestamp(df.index[0]))[:16],\n        "data_end": str(pd.Timestamp(df.index[-1]))[:16],\n        "message": message,\n    }\n    return row, curve\n\ndef news_themes(news):\n    themes = {\n        "energy / fuel": ["oil", "gas", "lng", "fuel", "opec", "refinery", "hormuz"],\n        "critical minerals": ["critical minerals", "rare earth", "lithium", "cobalt", "nickel", "graphite", "titanium"],\n        "AM / powder metallurgy": ["powder metallurgy", "additive manufacturing", "3d printing", "metal powder", "lpbf", "ebm", "hip"],\n        "aerospace / defense": ["aerospace", "defense", "missile", "aircraft", "procurement"],\n        "China / controls": ["china", "taiwan", "export control", "sanction", "tariff"],\n        "gold / silver": ["gold", "silver", "bullion", "safe haven"],\n    }\n    if news.empty:\n        return 0.0, pd.DataFrame({"theme": list(themes.keys()), "mentions": 0})\n    txt = " ".join(news["title"].fillna("").str.lower().tolist())\n    rows = []\n    score = 0\n    for th, words in themes.items():\n        c = sum(txt.count(w) for w in words)\n        rows.append({"theme": th, "mentions": c})\n        score += c\n    return min(score / 10, 4.0), pd.DataFrame(rows).sort_values("mentions", ascending=False)\n\ndef transmission_table(assets, features):\n    latest = features.iloc[-1]\n    rows = []\n    for a in assets:\n        name, group, link = asset_info(a)\n        rel = []\n        for m in relevant_materials_for_asset(a):\n            c = f"material:{m}"\n            if c in latest:\n                rel.append(c)\n        if "AM-PM:basket" in latest and any(x in str(link).lower() for x in ["powder", "additive", "aerospace", "titanium", "hip", "lpbf"]):\n            rel.append("AM-PM:basket")\n        if not rel:\n            rel = [c for c in latest.index if c.startswith("asset:") and a in c] + ["material stress"]\n        rel = [r for r in rel if r in latest.index]\n        score = float(latest[rel].mean()) if rel else 0.0\n        if score > .75:\n            reading = "shock is close to this asset"\n        elif score < -.75:\n            reading = "mapped inputs are easing"\n        else:\n            reading = "mixed / neutral"\n        rows.append({"asset": a, "name": name, "group": group, "material/process link": link, "transmission score": score, "reading": reading, "main path": " → ".join([x.replace("material:", "").replace("AM-PM:", "") for x in rel[:4]])})\n    return pd.DataFrame(rows).sort_values("transmission score", ascending=False)\n\ndef scenario_table(assets, copper, oil, gas, usd, vix, rare, ampm):\n    rows = []\n    for a in assets:\n        name, group, link = asset_info(a)\n        text = f"{group} {link}".lower()\n        score = 0\n        reasons = []\n        def add(cond, val, reason, producer=False):\n            nonlocal score\n            if cond:\n                score += val\n                reasons.append(reason)\n        add("copper" in text or "electrification" in text, copper * (1 if ("miner" in text or "mining" in text or "copper" in group) else -0.6), "copper sensitivity")\n        add("oil" in text or "fuel" in text or "energy" in text, oil * (1 if "energy" in group or a in ["USO", "XLE", "CL=F"] else -0.7), "oil/fuel channel")\n        add("gas" in text or "chemicals" in text or "smelting" in text or "powder" in text, gas * -0.7, "gas/process-energy cost")\n        add("rare" in text or "magnet" in text or "defense" in text or "semiconductor" in text, rare * (0.6 if "rare" in group else -0.5), "rare-earth/policy channel")\n        add("additive" in text or "powder" in text or "titanium" in text or "aerospace" in text, ampm * -0.55, "AM/PM input-cost channel")\n        score += usd * -0.4\n        score += vix * -0.6\n        stance = "scenario tailwind" if score > .5 else "scenario headwind" if score < -.5 else "scenario mixed"\n        rows.append({"asset": a, "scenario score": score, "reading": stance, "main reasons": ", ".join(reasons[:3]) or "macro beta"})\n    return pd.DataFrame(rows).sort_values("scenario score", ascending=False)\n\n# -----------------------------------------------------------------------------\n# UI\n# -----------------------------------------------------------------------------\nst.set_page_config(page_title="MatQuantLab", layout="wide")\nst.title("MatQuantLab")\nst.subheader("Material-driven industrial risk research")\nst.caption("Prices first. Then material stress, exposure transmission, model signals and event context for the assets you select.")\n\nwith st.sidebar:\n    st.header("Research setup")\n    universe = st.radio("Universe", ["Core assets", "Manufacturing companies", "Commodities/futures", "Everything"], index=3)\n    countries = []\n    if not COMPANIES.empty:\n        countries = st.multiselect("Country filter", sorted(COMPANIES["country"].dropna().unique().tolist()), default=[])\n    core = sorted(CORE.keys())\n    comp = sorted(COMPANIES["ticker"].astype(str).tolist())\n    fut = sorted(set(FUTURES.values()))\n    if universe == "Core assets":\n        choices = core\n    elif universe == "Manufacturing companies":\n        choices = comp\n    elif universe == "Commodities/futures":\n        choices = fut\n    else:\n        choices = sorted(set(core + comp + fut))\n    if countries and universe in ["Manufacturing companies", "Everything"]:\n        c = COMPANIES.loc[COMPANIES["country"].isin(countries), "ticker"].astype(str).tolist()\n        choices = sorted(set(c + (core + fut if universe == "Everything" else [])))\n    default_assets = [x for x in ["SPY", "COPX", "XME", "PRNT", "GLD", "SLV", "USO", "URA", "REMX"] if x in choices]\n    assets = st.multiselect("Assets to analyze", choices, default=default_assets[:8])\n    focus = st.selectbox("Focus asset", assets if assets else ["SPY"])\n    interval = st.selectbox("Data interval", ["1m", "5m", "15m", "30m", "60m", "1d"], index=5)\n    period = st.selectbox("History", ["1d", "5d", "1mo", "3mo", "6mo", "1y", "2y", "5y", "10y"], index=7)\n    model_choice = st.selectbox("Model selection", ["Adaptive", "Ridge", "ElasticNet", "Huber", "RandomForest", "ExtraTrees", "GradientBoosting", "Blended"], index=0)\n    horizon = st.slider("Forecast horizon, in bars", 1, 120, 10)\n    shock_window = st.slider("Shock window, in bars", 3, 150, 20)\n    fast = st.toggle("Fast mode", value=True)\n    st.markdown("**Event search**")\n    base_terms = st.multiselect(\n        "Base terms",\n        sorted(["critical minerals", "rare earth", "export controls", "sanctions", "Iran", "Strait of Hormuz", "Red Sea", "oil", "gas", "LNG", "gold", "silver", "China", "Taiwan", "titanium powder", "powder metallurgy", "LPBF", "EBM", "HIP", "aerospace backlog", "defense procurement"]),\n        default=["critical minerals", "rare earth", "export controls", "oil", "gas", "gold", "China", "powder metallurgy"],\n    )\n    extra = st.text_input("Extra event terms, comma separated", value="")\n    if extra:\n        base_terms += [x.strip() for x in extra.split(",") if x.strip()]\n    auto = st.toggle("Auto-refresh", value=False)\n    refresh = st.selectbox("Refresh every", [30, 60, 120, 300], index=1)\n    if st.button("Refresh now"):\n        st.cache_data.clear()\n        st.rerun()\n\nif auto:\n    time.sleep(refresh)\n    st.cache_data.clear()\n    st.rerun()\n\nif not assets:\n    st.warning("Select at least one asset.")\n    st.stop()\n\nactual_period = compatible_period(interval, period)\n\n# Build focused ticker list\nrelated_mats = []\nfor a in assets:\n    related_mats += relevant_materials_for_asset(a)\nif not related_mats or not fast:\n    related_mats += list(MATERIALS.keys())\nmat_tickers = []\nfor m in sorted(set(related_mats)):\n    mat_tickers += MATERIALS[m]["tickers"][:2 if fast else 3]\nessential = ["SPY", "^VIX", "DX-Y.NYB", "GC=F", "SI=F", "CL=F", "NG=F", "HG=F"]\ndownload_list = sorted(set(assets + mat_tickers + essential + ([] if fast else list(FUTURES.values()))))\nprices, missing = download_prices(download_list, actual_period, interval)\nif prices.empty:\n    st.error("No price data loaded. Try daily data, fewer assets or core assets.")\n    st.stop()\n\nfeatures, meta, used_window = build_features(prices, assets, shock_window)\nif features.empty:\n    st.error("No usable feature set. Try a longer history or daily interval.")\n    st.stop()\n\n# Tables, news, signals\nprice_table = returns_table(prices, [a for a in assets if a in prices.columns])\nsignals_rows, curves = [], {}\nfor a in assets:\n    row, curve = train_signal(a, prices, features, horizon, interval, model_choice)\n    if row:\n        name, group, link = asset_info(a)\n        row["name"] = name\n        row["group"] = group\n        row["material/process link"] = link\n        signals_rows.append(row)\n        curves[a] = curve\nsignals = pd.DataFrame(signals_rows)\nif not signals.empty:\n    trust_order = {"higher": 0, "medium": 1, "low-medium": 2, "low": 3}\n    signals["_o"] = signals["trust"].map(trust_order).fillna(9)\n    signals = signals.sort_values(["_o", "confidence"], ascending=[True, False]).drop(columns="_o")\nterms = build_news_terms(base_terms, assets)\nquery = make_query(terms)\nnews = pd.concat([gdelt_news(query, 60), google_news(query, 50)], ignore_index=True)\nevent_score, theme_table = news_themes(news)\ndrivers = current_driver_table(features, assets)\ntransmission = transmission_table(assets, features)\n\n# Header\nh1, h2, h3, h4, h5, h6 = st.columns(6)\nh1.metric("Focus", focus)\nlast_price_focus = price_table.loc[price_table["asset"] == focus, "last price"].iloc[0] if not price_table.empty and focus in price_table["asset"].values else np.nan\nh2.metric("Last price", f"{last_price_focus:,.2f}" if pd.notna(last_price_focus) else "n/a")\nh3.metric("Material stress", f"{features[\'material stress\'].iloc[-1]:.2f}" if "material stress" in features.columns else "n/a")\nh4.metric("Event risk", f"{event_score:.2f}")\nh5.metric("Signals", len(signals))\nh6.metric("Actual history", actual_period)\n\nif actual_period != period:\n    st.warning(f"{interval} data cannot reliably support {period}; using {actual_period} for this run.")\nif missing:\n    with st.expander("Missing or unavailable downloaded tickers", expanded=False):\n        st.write(", ".join(missing[:80]))\n\n# Brief sentence\nfocus_signal = ""\nif not signals.empty and focus in signals["asset"].values:\n    r = signals[signals["asset"] == focus].iloc[0]\n    focus_signal = f"{focus} signal: **{r[\'stance\']}**, trust **{r[\'trust\']}**, valid until about **{r[\'valid_until\']}**."\nelse:\n    focus_signal = f"{focus} did not produce a stable model signal under this setup."\ntop_driver = drivers.iloc[0]["driver"] if not drivers.empty else "no stable driver"\nst.info(f"For the selected assets, the main current driver is **{top_driver}**. {focus_signal} News query is built from your selected assets and event terms.")\n\ntabs = st.tabs(["Brief & Price Board", "Price Tape", "Material Pressure", "Exposure & Transmission", "Signals & Scenarios", "News & Events", "Data Quality"])\n\n# 1\nwith tabs[0]:\n    st.subheader("Brief and price board")\n    st.markdown("This page starts with actual prices and returns. Normalized charts are only used later for comparison.")\n    if price_table.empty:\n        st.warning("No selected price data loaded.")\n    else:\n        display = price_table.copy()\n        for c in ["1 bar", "5 bars", "20 bars", "realized vol", "drawdown"]:\n            display[c] = display[c].apply(fmt_pct)\n        display["z-score"] = display["z-score"].map(lambda x: "" if pd.isna(x) else f"{x:.2f}")\n        display["last price"] = display["last price"].map(lambda x: f"{x:,.2f}")\n        st.dataframe(display, use_container_width=True, hide_index=True)\n    st.markdown("### Focus asset reading")\n    name, group, link = asset_info(focus)\n    st.write(f"**{focus} — {name}**")\n    st.write(f"Group: **{group}**")\n    st.write(f"Material/process link: **{link}**")\n    rel_mats = relevant_materials_for_asset(focus)\n    if rel_mats:\n        st.write("Relevant material channels:")\n        for m in rel_mats:\n            spec = MATERIALS[m]\n            st.write(f"• **{m}:** {spec[\'family\']} — {spec[\'manufacturer_risk\']}")\n    if not signals.empty and focus in signals["asset"].values:\n        st.markdown("### Focus model message")\n        st.write(signals[signals["asset"] == focus].iloc[0]["message"])\n    st.markdown("### Most important current inputs")\n    for _, r in drivers.head(8).iterrows():\n        st.write("• " + material_interpretation(r["driver"], r["score"], focus))\n\n# 2\nwith tabs[1]:\n    st.subheader("Price tape")\n    st.markdown("Raw prices show actual market levels. Indexed charts show relative direction. Returns show performance over comparable bars.")\n    chart_mode = st.radio("Chart mode", ["Raw price: focus asset", "Raw price: selected assets", "Indexed comparison", "Return chart"], horizontal=True)\n    if chart_mode == "Raw price: focus asset":\n        cols = [focus] if focus in prices.columns else []\n        # add relevant futures for focus\n        for m in relevant_materials_for_asset(focus):\n            for t in MATERIALS[m]["tickers"][:1]:\n                if t in prices.columns:\n                    cols.append(t)\n        cols = list(dict.fromkeys(cols))\n        chart_lines(prices[cols], f"{focus}: raw price with relevant material proxies", "actual price", normalize=False)\n    elif chart_mode == "Raw price: selected assets":\n        cols = [a for a in assets if a in prices.columns][:10]\n        chart_lines(prices[cols], "Selected assets — raw prices", "actual price", normalize=False)\n        st.caption("Raw multi-asset charts can look uneven when assets have different price scales. Use indexed comparison for relative performance.")\n    elif chart_mode == "Indexed comparison":\n        cols = [a for a in assets if a in prices.columns][:12]\n        chart_lines(prices[cols], "Selected assets — indexed to 1.0", "indexed price", normalize=True)\n    else:\n        ret = prices[[a for a in assets if a in prices.columns]].pct_change().cumsum()\n        chart_lines(ret, "Selected assets — cumulative log-style return proxy", "cumulative return proxy", normalize=False)\n\n    st.markdown("### Commodity and futures board")\n    fut_rows = []\n    for instr, tic in FUTURES.items():\n        if tic in prices.columns:\n            s = prices[tic].dropna()\n            if not s.empty:\n                fut_rows.append({\n                    "instrument": instr,\n                    "ticker": tic,\n                    "last": float(s.iloc[-1]),\n                    "5 bars": float(s.iloc[-1] / s.iloc[-6] - 1) if len(s) > 6 else np.nan,\n                    "20 bars": float(s.iloc[-1] / s.iloc[-21] - 1) if len(s) > 21 else np.nan,\n                    "state": "extended up" if (zscore(s.pct_change(), 20).dropna().iloc[-1] if len(s) > 30 and not zscore(s.pct_change(), 20).dropna().empty else 0) > 1 else "extended down" if (zscore(s.pct_change(), 20).dropna().iloc[-1] if len(s) > 30 and not zscore(s.pct_change(), 20).dropna().empty else 0) < -1 else "normal",\n                })\n    fut_df = pd.DataFrame(fut_rows)\n    if not fut_df.empty:\n        show = fut_df.copy()\n        show["last"] = show["last"].map(lambda x: f"{x:,.2f}")\n        show["5 bars"] = show["5 bars"].apply(fmt_pct)\n        show["20 bars"] = show["20 bars"].apply(fmt_pct)\n        st.dataframe(show, use_container_width=True, hide_index=True)\n        selected_fut = st.multiselect("Chart futures/proxies", fut_df["ticker"].tolist(), default=fut_df["ticker"].tolist()[:8])\n        if selected_fut:\n            chart_lines(prices[selected_fut], "Futures/proxies — raw prices", "actual price", normalize=False)\n            chart_lines(prices[selected_fut], "Futures/proxies — indexed comparison", "indexed", normalize=True)\n    else:\n        st.info("No futures/proxy series loaded. Turn off Fast mode or select commodity/future assets.")\n\n# 3\nwith tabs[2]:\n    st.subheader("Material pressure")\n    st.markdown("This section avoids clustered spaghetti charts. It uses current pressure bars plus focused time-series for the selected asset.")\n    rel_cols = []\n    for m in relevant_materials_for_asset(focus):\n        c = f"material:{m}"\n        if c in features.columns:\n            rel_cols.append(c)\n    for c in ["AM-PM:feedstock", "AM-PM:process energy", "AM-PM:powder metallurgy inputs", "AM-PM:basket", "macro:VIX", "macro:USD"]:\n        if c in features.columns:\n            rel_cols.append(c)\n    rel_cols = list(dict.fromkeys(rel_cols))\n    if rel_cols:\n        focus_press = pd.DataFrame({"driver": rel_cols, "score": [float(features[c].iloc[-1]) for c in rel_cols]})\n        chart_bar(focus_press, "score", "driver", f"{focus}: material and macro pressure now")\n        st.markdown("**What the bars mean for the focus asset:**")\n        for _, r in focus_press.sort_values("score", key=lambda s: s.abs(), ascending=False).head(8).iterrows():\n            st.write("• " + material_interpretation(r["driver"], r["score"], focus))\n        chart_lines(features[rel_cols], f"{focus}: relevant stress paths through time", "z-score")\n        st.caption("Use this chart to see whether the stress is new, persistent, fading, or reversing.")\n    else:\n        st.info("No mapped material paths for this focus asset.")\n\n    st.markdown("### All current drivers")\n    chart_bar(drivers.head(22).sort_values("score"), "score", "driver", "Largest current drivers in the selected universe")\n    st.caption("Positive does not mean good. It means upward or unusually strong pressure. The effect depends on whether the asset is a producer, manufacturer or hedge.")\n\n# 4\nwith tabs[3]:\n    st.subheader("Exposure and transmission")\n    st.markdown("Transmission score routes current stress through the selected asset\'s material/process map. It is not a price forecast.")\n    st.dataframe(transmission, use_container_width=True, hide_index=True)\n    chart_bar(transmission, "transmission score", "asset", "Transmission score for selected assets")\n    st.markdown("### Transmission interpretation")\n    for _, r in transmission.iterrows():\n        st.write(f"• **{r[\'asset\']}**: {r[\'reading\']} through **{r[\'main path\']}**.")\n    st.markdown("### Selected asset material map")\n    map_rows = []\n    for a in assets:\n        name, group, link = asset_info(a)\n        map_rows.append({"asset": a, "name": name, "group": group, "material/process link": link, "mapped materials": ", ".join(relevant_materials_for_asset(a))})\n    st.dataframe(pd.DataFrame(map_rows), use_container_width=True, hide_index=True)\n\n# 5\nwith tabs[4]:\n    st.subheader("Signals and scenarios")\n    if signals.empty:\n        st.warning("No model signals for this setup. Try daily data, longer history, fewer assets, or common tickers.")\n    else:\n        signal_cols = ["asset", "name", "group", "stance", "strength", "trust", "model", "prediction", "confidence", "test_corr", "driver_1", "driver_2", "driver_3", "horizon_bars", "valid_until", "validity", "message"]\n        st.dataframe(signals[signal_cols], use_container_width=True, hide_index=True)\n        st.markdown("### Inspect signal")\n        inspect = st.selectbox("Signal to inspect", signals["asset"].tolist(), index=signals["asset"].tolist().index(focus) if focus in signals["asset"].tolist() else 0)\n        row = signals[signals["asset"] == inspect].iloc[0]\n        a, b, c, d = st.columns(4)\n        a.metric("Stance", row["stance"])\n        b.metric("Trust", row["trust"])\n        c.metric("Test corr.", f"{row[\'test_corr\']:.3f}")\n        d.metric("Valid until", row["valid_until"])\n        st.write(row["message"])\n        if inspect in curves:\n            curve = curves[inspect]\n            chart_lines(curve[["prediction", "future_return"]], f"{inspect}: model prediction vs realized later return", "return")\n            st.caption("If the lines repeatedly disagree, the model has low practical value even if the current signal looks strong.")\n            chart_lines(curve[["diagnostic_curve"]], f"{inspect}: diagnostic curve", "index")\n            st.caption("Diagnostic only, not a tradable backtest.")\n\n    st.markdown("### Scenario test")\n    st.caption("Change the shocks and see which selected assets are helped or hurt by the mapped material/process exposure.")\n    sc1, sc2, sc3, sc4 = st.columns(4)\n    copper = sc1.slider("Copper shock", -2.0, 2.0, 0.0, .25)\n    oil = sc2.slider("Oil/fuel shock", -2.0, 2.0, 0.0, .25)\n    gas = sc3.slider("Gas/process-energy shock", -2.0, 2.0, 0.0, .25)\n    usd = sc4.slider("USD shock", -2.0, 2.0, 0.0, .25)\n    sc5, sc6, sc7 = st.columns(3)\n    vix = sc5.slider("VIX/risk-off shock", -2.0, 2.0, 0.0, .25)\n    rare = sc6.slider("Rare-earth/policy shock", -2.0, 2.0, 0.0, .25)\n    ampm = sc7.slider("AM/PM input-cost shock", -2.0, 2.0, 0.0, .25)\n    scen = scenario_table(assets, copper, oil, gas, usd, vix, rare, ampm)\n    st.dataframe(scen, use_container_width=True, hide_index=True)\n    chart_bar(scen, "scenario score", "asset", "Scenario score by selected asset")\n\n# 6\nwith tabs[5]:\n    st.subheader("News and events")\n    st.markdown("The query is built from selected assets, company/material links and your extra terms.")\n    st.code(query)\n    n1, n2, n3 = st.columns(3)\n    n1.metric("Event score", f"{event_score:.2f}")\n    n2.metric("Headlines", len(news))\n    n3.metric("Terms", len(terms))\n    if not theme_table.empty:\n        st.dataframe(theme_table, use_container_width=True, hide_index=True)\n        chart_bar(theme_table.rename(columns={"mentions": "score", "theme": "driver"}), "score", "driver", "Event theme intensity")\n    if news.empty:\n        st.info("No news returned. Use fewer or broader event terms.")\n    else:\n        st.dataframe(news[["title", "source", "date", "url"]].head(120), use_container_width=True, hide_index=True)\n    st.markdown("### How to connect news to the model")\n    st.write("• If news themes match the model drivers, the signal is more coherent.")\n    st.write("• If news risk is high but prices are calm, watch for delayed transmission.")\n    st.write("• If price stress is high but news is quiet, the move may be positioning, rates, dollar, or technical rather than event-driven.")\n\n# 7\nwith tabs[6]:\n    st.subheader("Data quality")\n    q1, q2, q3, q4 = st.columns(4)\n    q1.metric("Requested history", period)\n    q2.metric("Actual history", actual_period)\n    q3.metric("Price rows", len(prices))\n    q4.metric("Feature rows", len(features))\n    st.write(f"Price coverage: `{prices.index[0]}` → `{prices.index[-1]}`")\n    st.write(f"Effective shock window: `{used_window}`")\n    st.markdown("### Downloaded prices")\n    st.dataframe(prices.tail(120), use_container_width=True)\n    st.markdown("### Feature metadata")\n    st.dataframe(meta, use_container_width=True, hide_index=True)\n    st.markdown("### Latest feature values")\n    latest_features = features.iloc[-1].rename("latest score").reset_index().rename(columns={"index": "feature"}).sort_values("latest score", key=lambda s: s.abs(), ascending=False)\n    st.dataframe(latest_features.head(100), use_container_width=True, hide_index=True)\n\nst.divider()\nst.caption("Research workbench. Free public market/news data may be delayed or unavailable. Signals are diagnostics, not investment advice.")\n')
